{{page-title "Step1"}}
<div class="panel">
  <div class="row">
    <div class="col-sm">
<MultiSelect @elements={{this.dispoItems}}
             @onChange={{this.changeDispo}} class="form-control"/>
    </div>
    <div class="col-sm">
      <button {{action "add" this.dispoItems this.includedItems this.dispoItems_}}class="btn btn-primary">
        <i class="fas fa-forward"></i>
      </button>
    </div>
    <div class="col-sm">
  <MultiSelect @elements={{this.includedItems}}
               @onChange={{this.changeIncluded}} class="form-control"/>
    </div>
  </div>
</div>
{{outlet}}